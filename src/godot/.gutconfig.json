{
  "_comment": "GUT 默认配置。固化测试目录，避免依赖命令行手敲 -gdir 而放错位置（放在 res:// 之外的测试会被静默跳过）。",
  "dirs": ["res://tests"],
  "include_subdirs": true,
  "prefix": "test_",
  "suffix": ".gd",
  "should_exit": true,
  "log_level": 1
}
